#if false
// Deprecated duplicate. Intentionally disabled to avoid type collisions; keep the test-only version in Utils/LogCapture.cs
using System; using System.Collections.Generic; using UnityEngine;
public sealed class LogCapture : IDisposable { public void Dispose(){} }
#endif
